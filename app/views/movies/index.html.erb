<ul>
  <% @movies.each do |m| %>
    <li>
      <%= m.title %>
      (<%= m.rating %>):
      <%= number_to_currency(m.total_gross, precision: 0) %>
    </li>
    <p>
      <%= m.description %>
    </p>
    <p>
      <%= m.released_on %>
    </p>
  <% end %>
</ul>
